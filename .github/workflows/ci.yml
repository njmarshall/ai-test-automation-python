name: CI — AI Test Automation Suite

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  # ------------------------------------------------------------------ #
  #  API Tests — Patient, Encounter, Observation                        #
  # ------------------------------------------------------------------ #
  api-tests:
    name: FHIR API Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run FHIR API tests
        run: |
          pytest projects/healthcare_fhir/api/tests/ \
            -v \
            -m healthcare \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  UI Tests — Playwright HAPI FHIR explorer                          #
  # ------------------------------------------------------------------ #
  ui-tests:
    name: FHIR UI Tests (Playwright)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: playwright install chromium --with-deps

      - name: Run FHIR UI tests
        run: |
          pytest projects/healthcare_fhir/ui/tests/ \
            -v \
            -m ui \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  PetStore API Tests                                                 #
  # ------------------------------------------------------------------ #
  petstore-tests:
    name: PetStore API Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run PetStore API tests
        run: |
          pytest projects/petstore/api/tests/ \
            -v \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  Insurance API Tests                                                #
  # ------------------------------------------------------------------ #
  insurance-tests:
    name: Insurance API Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Insurance API tests
        run: |
          pytest projects/insurance/api/tests/ \
            -v \
            -m insurance \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  Fintech API Tests                                                  #
  # ------------------------------------------------------------------ #
  fintech-tests:
    name: Fintech Market Data Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Fintech API tests
        run: |
          pytest projects/fintech/api/tests/ \
            -v \
            -m fintech \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  Insurance UI Tests                                                 #
  # ------------------------------------------------------------------ #
  insurance-ui-tests:
    name: Insurance UI Tests (Playwright)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: playwright install chromium --with-deps

      - name: Run Insurance UI tests
        run: |
          pytest projects/insurance/ui/tests/ \
            -v \
            -m "insurance and ui" \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  Fintech UI Tests                                                   #
  # ------------------------------------------------------------------ #
  fintech-ui-tests:
    name: Fintech UI Tests (Playwright)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: playwright install chromium --with-deps

      - name: Run Fintech UI tests
        run: |
          pytest projects/fintech/ui/tests/ \
            -v \
            -m "fintech and ui" \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  PetStore UI Tests                                                  #
  # ------------------------------------------------------------------ #
  petstore-ui-tests:
    name: PetStore UI Tests (Playwright)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: playwright install chromium --with-deps

      - name: Run PetStore UI tests
        run: |
          pytest projects/petstore/ui/tests/ \
            -v \
            -m "petstore and ui" \
            --tb=short \
            --no-header

  # ------------------------------------------------------------------ #
  #  Async Polling Tests                                                #
  # ------------------------------------------------------------------ #
  async-polling-tests:
    name: Async Polling Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Async Polling tests
        run: |
          pytest projects/fintech/api/tests/test_async_polling.py \
            -v \
            --tb=short \
            --no-header
